name: chorus-enformer
channels:
  - conda-forge
  - bioconda
dependencies:
  # Core Python

  - python=3.10
  
  # Essential scientific computing
  - numpy>=1.21.0
  - pandas>=1.3.0
  - scipy>=1.7.0
  
  # Core bioinformatics
  - pysam>=0.19.0
  - biopython>=1.79
  - pyfaidx>=0.7.0
  
  # Utilities
  - tqdm>=4.62.0
  - h5py>=3.6.0
  
  # Visualization (for Enformer's contribution scores)
  - matplotlib>=3.5.0
  - seaborn>=0.11.0
  - matplotlib-inline>=0.2.1
  
  # TensorFlow ecosystem
  - pip:
    # Enformer specific dependencies
    - tensorflow>=2.8.0,<2.15.0
    - tensorflow-hub>=0.12.0
    - tensorflow-datasets>=4.0.0
    - setuptools<81  # tensorflow_hub requires pkg_resources