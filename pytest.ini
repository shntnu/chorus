[pytest]
markers =
    integration: slow end-to-end tests that hit the network, download
        models/CDFs from HuggingFace/ENCODE, or spawn the chorus-mcp
        subprocess. Skipped by default in the fast suite; run with
        `pytest -m integration` when maintainer verification is needed.
